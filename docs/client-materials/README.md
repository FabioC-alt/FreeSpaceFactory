# Client-supplied materials

`PREVENTIVI PACCHETTI_17092026.docx` — the pricing sheet the client sent (via a call transcript
and this file) for the "Chiavi in Mano" package section. This is the **simplified/updated**
version; it superseded an earlier draft with the same three price points (€2.000 / €4.500 /
€6.000) but more verbose wording. The content of this file is what's reflected in
`src/pages/chiavi-in-mano.astro` (the `packages` array).

Per the client, these prices are provisional ("potrebbero cambiare ma per ora vanno bene questi" —
might change but these are fine for now) — see the root [HANDOFF.md](../../HANDOFF.md) for the
full list of open items.
